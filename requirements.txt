fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
# python-multipart: requerido por FastAPI para leer Form()/UploadFile() -
# usado por el endpoint de subida de fotos del upsell de video (Fase 2, ver
# ESTADO_PROYECTO.md / plan de expansion a EE.UU.). Se agrega ya de una
# aunque el endpoint todavia no exista, para no tener que tocar requirements
# de nuevo en la Fase 2.
python-multipart==0.0.9
# reportlab: genera al vuelo el PDF descargable/imprimible de la letra
# aprobada (ver app/pdf_client.py) - pura Python, sin dependencias nativas
# externas, para no depender de que Render tenga instalado wkhtmltopdf/etc.
reportlab==4.2.5
